version: 2

backend:
  phases:
    preBuild:
      commands:
        - node -v
        - npm -v
        - npm config set fund false
        - npm config set audit false
        - npm config set progress false
        - npm install --omit=dev --no-save --cache .npm --prefer-offline --no-audit --no-fund
        
    build:
      commands:
        - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID

  cache:
    paths:
      - .npm/**/*
      - .amplify/**/*

frontend:
  phases:
    preBuild:
      commands:
        - node -v
        - npm -v
        - npm config set fund false
        - npm config set audit false
        - npm config set progress false
        - npm install --no-save --cache .npm --prefer-offline --no-audit --no-fund
    build:
      commands:
        - npm run lint
        - npm run format:check
        - npm run test:coverage -- --watch=false --ci --passWithNoTests
        - npm run build -- --configuration production

  artifacts:
    baseDirectory: dist/starter-scaffolding-angular/browser
    files:
      - "**/*"

  cache:
    paths:
      - .npm/**/*
      - .angular/cache/**/*

  environment:
    variables:
      CI: "true"
      NODE_ENV: "production"
      NG_CLI_ANALYTICS: "false"
      NODE_OPTIONS: "--max-old-space-size=6144"